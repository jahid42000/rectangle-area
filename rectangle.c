#include <stdio.h>
int main()
{

    float length,wade,area;
    printf(" enter length and area :");
    scanf("%f%f",&length,&wade);
    area = length*wade;
    printf("ractngle area is :%.2f",area);


}
